<?xml version="1.0" encoding="utf-8"?>
<!--
TODO(디버그 검증용 임시 코드, 확인 끝나면 삭제): TokenCorruptionDebugReceiver 등록.
app/src/debug 전용이라 release 빌드 매니페스트에는 병합되지 않는다.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <receiver
            android:name=".debug.TokenCorruptionDebugReceiver"
            android:exported="false" />
    </application>
</manifest>
