package com.example.pickii.domain.model

/**
 * 일정 태그에 사용할 색상 종류다.
 */
enum class ScheduleColorType {
    RED,
    ORANGE,
    YELLOW,
    GREEN,
    BLUE,
    PURPLE,
    PINK,
    GRAY,
    BLACK,
}